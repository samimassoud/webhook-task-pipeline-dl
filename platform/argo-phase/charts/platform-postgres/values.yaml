# platform-postgres/values.yaml
sealedAdminSecret:
  encryptedDatabaseUrl: ""
  name: ""
# Configuration published for other platform components.
configMap:
  name: postgres-configs

# Required database identity.
# These are intentionally empty defaults and must be supplied
# by the environment-specific values file.
postgresql:
  auth:
    username: ""
    database: ""